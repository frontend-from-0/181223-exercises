// In HTML, use input type checkbox element to implement the dark/light theme switch. Make sure to give id 'themeSwitch' to that input element.
const themeSwitch = document.getElementById('themeSwitch');

themeSwitch.addEventListener('change', () => {
  // Only add your code below this line:
  if (themeSwitch.checked) {
    // Implement logic for when the input is checked (e.g. update classes to use dark mode)

  } else {
    // Implement logic for when the input is checked (e.g. update classes to use light mode)

  }
  // Only add your code above this line
});
